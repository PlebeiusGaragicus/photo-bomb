"""Bundled application assets (icons, etc.).

This is a package (rather than a plain directory) so that `importlib.resources`
can locate files inside it identically in dev mode and inside a frozen
PyInstaller bundle.
"""
